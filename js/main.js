var letter = prompt('Enter a letter.');

var counter = function (name, times) {
  var i;

  for (i = 0; 1 < times; i++) {
    document.write('<br>')
  }

  document.write('<br>');

};

counter (letter, 1);
counter (letter, 2);
counter (letter, 3);
counter (letter, 4);
counter (letter, 5);
counter (letter, 6);
counter (letter, 7);
counter (letter, 8);
counter (letter, 9);
